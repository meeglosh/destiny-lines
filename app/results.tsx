
import React, { useState } from 'react';
import { View, Text, StyleSheet, ScrollView, Pressable, Alert } from 'react-native';
import { router, Stack, useLocalSearchParams } from 'expo-router';
import * as Sharing from 'expo-sharing';
import { commonStyles, colors, textStyles, buttonStyles } from '@/styles/commonStyles';
import { Button } from '@/components/button';
import { LinearGradient } from 'expo-linear-gradient';
import AdBanner from '@/components/AdBanner';

// Mock AI palm reading result
const mockPalmReading = {
  lifeLine: {
    icon: '🌿',
    title: 'Life Line',
    content: 'Your life line curves broadly around the base of your thumb and is fairly strong. In palmistry, that suggests good stamina, a strong constitution, and the ability to bounce back from setbacks. You\'re likely someone who doesn\'t stay down for long — resilience is your quiet superpower.'
  },
  headLine: {
    icon: '🧠',
    title: 'Head Line',
    content: 'Your head line runs fairly straight across the palm, not overly curved. This is often read as a sign of clear, practical thinking and a rational approach to life. You weigh decisions carefully, preferring logic over impulse. People probably see you as steady and reliable when it comes to making choices.'
  },
  heartLine: {
    icon: '❤️',
    title: 'Heart Line',
    content: 'Your heart line sits higher and looks straighter than deeply curved. In palmistry, that suggests you\'re not overly dramatic in love — you value stability, trust, and loyalty. You might not be wildly expressive all the time, but when you commit, you commit deeply.'
  },
  handShape: {
    icon: '✋',
    title: 'Shape of the Hand',
    content: 'Your hand looks rectangular with longer fingers — this is sometimes called an "air hand." In palmistry, that\'s tied to curiosity, communication, and creativity. You probably like exploring ideas, connecting dots, and solving problems in inventive ways.'
  },
  overall: {
    icon: '🌟',
    title: 'Overall Reading',
    content: 'Your palm suggests someone who\'s resilient, thoughtful, and steady in both practical and emotional life. You balance a curious, creative mind with a strong need for groundedness. Others likely lean on you for clarity and consistency — but you also have a restless streak that keeps you seeking out new ideas and projects.'
  }
};

export default function ResultsScreen() {
  const params = useLocalSearchParams();
  const [isSharing, setIsSharing] = useState(false);

  console.log('ResultsScreen rendered with params:', params);

  const shareReading = async () => {
    console.log('Sharing palm reading');
    setIsSharing(true);

    try {
      const readingText = `🔮 My Destiny Lines Palm Reading 🔮\n\n` +
        `${mockPalmReading.lifeLine.icon} ${mockPalmReading.lifeLine.title}\n${mockPalmReading.lifeLine.content}\n\n` +
        `${mockPalmReading.headLine.icon} ${mockPalmReading.headLine.title}\n${mockPalmReading.headLine.content}\n\n` +
        `${mockPalmReading.heartLine.icon} ${mockPalmReading.heartLine.title}\n${mockPalmReading.heartLine.content}\n\n` +
        `${mockPalmReading.handShape.icon} ${mockPalmReading.handShape.title}\n${mockPalmReading.handShape.content}\n\n` +
        `${mockPalmReading.overall.icon} ${mockPalmReading.overall.title}\n${mockPalmReading.overall.content}\n\n` +
        `Generated by Destiny Lines - AI Palm Reading App`;

      const isAvailable = await Sharing.isAvailableAsync();
      if (isAvailable) {
        await Sharing.shareAsync('data:text/plain;base64,' + btoa(readingText), {
          mimeType: 'text/plain',
          dialogTitle: 'Share Your Palm Reading',
        });
        console.log('Reading shared successfully');
      } else {
        Alert.alert('Sharing not available', 'Sharing is not available on this device.');
      }
    } catch (error) {
      console.log('Error sharing reading:', error);
      Alert.alert('Error', 'Failed to share reading. Please try again.');
    } finally {
      setIsSharing(false);
    }
  };

  const saveReading = () => {
    console.log('Saving reading to history');
    Alert.alert(
      'Reading Saved',
      'Your palm reading has been saved to your history.',
      [{ text: 'OK' }]
    );
  };

  const getNewReading = () => {
    console.log('Getting new reading');
    router.push('/camera');
  };

  return (
    <View style={commonStyles.wrapper}>
      <Stack.Screen 
        options={{ 
          title: 'Your Palm Reading',
          headerStyle: { backgroundColor: colors.background },
          headerTintColor: colors.text,
          headerLeft: () => (
            <Pressable onPress={() => router.push('/')} style={styles.backButton}>
              <Text style={styles.backButtonText}>← Home</Text>
            </Pressable>
          ),
        }} 
      />
      
      <LinearGradient
        colors={['#F5F1E8', '#E8DCC6', '#D4C4A8']}
        style={styles.container}
      >
        <ScrollView 
          style={styles.scrollView}
          contentContainerStyle={styles.scrollContent}
          showsVerticalScrollIndicator={false}
        >
          {/* Header */}
          <View style={styles.header}>
            <Text style={styles.title}>🔮 Your Destiny Revealed</Text>
            <Text style={styles.subtitle}>AI Palm Reading Results</Text>
          </View>

          {/* Ad Banner */}
          <AdBanner />

          {/* Reading Sections */}
          <View style={styles.readingContainer}>
            {Object.entries(mockPalmReading).map(([key, section]) => (
              <View key={key} style={styles.sectionCard}>
                <View style={styles.sectionHeader}>
                  <Text style={styles.sectionIcon}>{section.icon}</Text>
                  <Text style={styles.sectionTitle}>{section.title}</Text>
                </View>
                <Text style={styles.sectionContent}>{section.content}</Text>
              </View>
            ))}
          </View>

          {/* Action Buttons */}
          <View style={styles.actionSection}>
            <Button
              onPress={shareReading}
              loading={isSharing}
              disabled={isSharing}
              style={styles.primaryButton}
              textStyle={styles.primaryButtonText}
            >
              {isSharing ? 'Sharing...' : '📤 Share Reading'}
            </Button>
            
            <Button
              onPress={saveReading}
              style={styles.secondaryButton}
              textStyle={styles.secondaryButtonText}
            >
              💾 Save to History
            </Button>
            
            <Button
              onPress={getNewReading}
              style={styles.tertiaryButton}
              textStyle={styles.tertiaryButtonText}
            >
              🔄 New Reading
            </Button>
          </View>
        </ScrollView>
      </LinearGradient>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  scrollView: {
    flex: 1,
  },
  scrollContent: {
    paddingHorizontal: 20,
    paddingTop: 20,
    paddingBottom: 40,
  },
  backButton: {
    paddingHorizontal: 16,
    paddingVertical: 8,
  },
  backButtonText: {
    color: colors.text,
    fontSize: 16,
    fontWeight: '500',
  },
  header: {
    alignItems: 'center',
    marginBottom: 20,
  },
  title: {
    ...textStyles.title,
    fontSize: 28,
    marginBottom: 8,
  },
  subtitle: {
    ...textStyles.subtitle,
    fontSize: 16,
  },
  readingContainer: {
    marginBottom: 30,
  },
  sectionCard: {
    ...commonStyles.card,
    marginBottom: 16,
  },
  sectionHeader: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 12,
  },
  sectionIcon: {
    fontSize: 24,
    marginRight: 12,
  },
  sectionTitle: {
    fontSize: 20,
    fontWeight: 'bold',
    color: colors.text,
    flex: 1,
  },
  sectionContent: {
    ...textStyles.body,
    textAlign: 'left',
  },
  actionSection: {
    paddingTop: 20,
  },
  primaryButton: {
    ...buttonStyles.primary,
    marginBottom: 12,
  },
  primaryButtonText: {
    ...textStyles.buttonPrimary,
  },
  secondaryButton: {
    ...buttonStyles.secondary,
    marginBottom: 12,
  },
  secondaryButtonText: {
    ...textStyles.buttonSecondary,
  },
  tertiaryButton: {
    ...buttonStyles.tertiary,
  },
  tertiaryButtonText: {
    color: colors.textSecondary,
    fontSize: 16,
    fontWeight: '500',
    textAlign: 'center',
    textDecorationLine: 'underline',
  },
});
