# macOS
.DS_Store

# Xcode project, generated by XcodeGen from project.yml — do not commit, do not hand-edit
*.xcodeproj/
*.xcworkspace/
xcuserdata/

# Build artifacts
build/
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.swiftpm/
.build/

# Secrets and credentials — must never be committed (see CLAUDE.md §1)
.env
.env.*
*.p8
*.p12
*.mobileprovision
*.pem
*.cer
*.key
GoogleService-Info.plist

# Supabase CLI local state
supabase/.branches/
supabase/.temp/
.supabase/

# Node (edge function tooling, if used)
node_modules/

# Calibration set — photographs of people's hands. Never commit these (§6.2a).
calibration/
